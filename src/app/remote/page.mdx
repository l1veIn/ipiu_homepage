import { Libraries } from '@/components/Libraries'

export const metadata = {
  title: '远程访问',
  description:
    'Protocol offers fine-tuned JavaScript, Ruby, PHP, Python, and Go libraries to make your life easier and give you the best experience when consuming the API.',
}

export const sections = [
  { title: '三种远程访问方式', id: 'official-libraries' },
]

# 远程访问

调试代码并且运行是一个不断尝试不断观察的过程，这就要求实验人员能够随时访问服务器以及服务器中的代码。每次都通过SSH连接到服务器的命令后既不安全也不快捷，更缺少对代码编辑的支持。所以合理的远程访问策略可以大大加快你的科研进度。（拜托不要再用向日葵了！） {{ className: 'lead' }}

<Libraries />
